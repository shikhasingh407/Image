'use strict';

/**
 * @ngdoc function
 * @name webApp.controller:ArtistmoduleCtrl
 * @description
 * # ArtistmoduleCtrl
 * Controller of the webApp
 */
angular.module('webApp')
  .controller('ArtistmoduleCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
