'use strict';

/**
 * @ngdoc function
 * @name webApp.controller:BlogmoduleCtrl
 * @description
 * # BlogmoduleCtrl
 * Controller of the webApp
 */
angular.module('webApp')
  .controller('BlogmoduleCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
